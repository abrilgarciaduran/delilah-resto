const db_host = 'localhost';
const db_name = 'delilah_resto';
const db_user = 'root';
const db_password = '';
const db_port = 3306;

module.exports = {
    db_host,
    db_name,
    db_user,
    db_password,
    db_port
}